[project]
name = "jaxgpt"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["jax>=0.4.30", "optax", "numpy"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jaxgpt"]